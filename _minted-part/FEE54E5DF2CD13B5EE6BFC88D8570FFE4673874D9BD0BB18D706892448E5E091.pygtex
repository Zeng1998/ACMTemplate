\begin{Verbatim}[commandchars=\\\{\}]
\PYG{c+cm}{/*}
\PYG{c+cm}{ * 0. poj2528: 离散化(注意线段相接的判断)+染色(区间更新，查询时暴力到叶子)}
\PYG{c+cm}{ * 1. zoj1610: 染色(区间更新，查询暴力到叶子，维护前一个颜色，判断颜色是否中断)}
\PYG{c+cm}{ * 2. hdu3974: dfs序建线段树(dfs一遍记录每个点‘进去’和‘出来’的时间戳，看成一个区间建树)}
\PYG{c+cm}{ *             区间维护的是子树的状态，因为对于每一个节点来说，进和出的中间就是访问子树}
\PYG{c+cm}{ * 3. hdu4614: 常规线段树+二分}
\PYG{c+cm}{ */} 
\end{Verbatim}
